# Cartilage OS - Dedicated Appliance mkinitcpio Configuration
# Ensures erofs, virtio, overlay, and zram modules are always baked in.

MODULES=(erofs virtio_blk virtio_pci virtio_net virtio_gpu virtio_dma_buf overlay zram zstd loop)
BINARIES=()
FILES=()
HOOKS=(base udev modconf block filesystems)
COMPRESSION="zstd"
